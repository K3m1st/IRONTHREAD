{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ironthread.local/schemas/handoff.schema.json",
  "title": "Oracle to Elliot Handoff",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "operation",
    "timestamp",
    "phase",
    "elliot_authorized",
    "scope",
    "context_files",
    "primary_path",
    "backup_path",
    "vulnerability_primitive"
  ],
  "properties": {
    "operation": {
      "type": "string",
      "minLength": 1
    },
    "timestamp": {
      "type": "string",
      "format": "date-time"
    },
    "phase": {
      "type": "string",
      "const": "EXPLOITATION"
    },
    "elliot_authorized": {
      "type": "boolean",
      "const": true
    },
    "scope": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "objective",
        "in_scope",
        "out_of_scope",
        "stop_conditions",
        "max_attempts_per_path",
        "max_turns"
      ],
      "properties": {
        "objective": {
          "type": "string",
          "minLength": 1
        },
        "in_scope": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "out_of_scope": {
          "type": "string",
          "minLength": 1
        },
        "stop_conditions": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "max_attempts_per_path": {
          "type": "integer",
          "minimum": 1,
          "maximum": 5
        },
        "max_turns": {
          "type": "integer",
          "minimum": 5,
          "maximum": 40,
          "description": "Total turn budget for this deployment. Oracle sets based on CVE complexity. ELLIOT hard-stops when exhausted."
        }
      }
    },
    "context_files": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "primary_path": {
      "type": "string",
      "minLength": 1
    },
    "backup_path": {
      "type": "string",
      "minLength": 1
    },
    "vulnerability_primitive": {
      "type": "object",
      "required": ["primitive", "delivery_forms"],
      "properties": {
        "primitive": {
          "type": "string",
          "minLength": 1,
          "description": "What the attacker controls — the underlying input that enables the vulnerability"
        },
        "delivery_forms": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "ALL valid forms of the controlled input, not just what PoCs show"
        },
        "defenses_observed": {
          "type": "string",
          "description": "What filtering or defenses the target applies to this input"
        },
        "untested_forms": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Delivery forms not yet attempted — ELLIOT should prioritize these"
        }
      }
    },
    "complexity": {
      "type": "string",
      "enum": ["trivial", "standard", "complex"],
      "description": "Oracle's assessment of exploit complexity. Guides Elliot's expectations and turn budget pacing."
    },
    "process_model": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "foreground": {
          "type": "string",
          "description": "What runs in the foreground (e.g., 'reverse shell listener', 'SSH session')"
        },
        "background": {
          "type": "string",
          "description": "What runs in the background (e.g., 'chisel tunnel on :8081', 'none')"
        },
        "critical_note": {
          "type": "string",
          "description": "Process lifecycle warning (e.g., 'do not ctrl-c the listener — shell dies')"
        }
      },
      "description": "Describes the process model Elliot inherits or must maintain. Prevents foreground/background bugs."
    },
    "opsec_profile": {
      "type": "string",
      "enum": ["LOUD", "MODERATE", "GHOST"],
      "default": "LOUD",
      "description": "Engagement noise profile. LOUD = pentest speed, detection acceptable. MODERATE = avoid automated IDS/WAF alerts. GHOST = zero detection, ops take days. See schemas/OPSEC_PROFILES.md for tool-specific rate limits per profile."
    },
    "notes": {
      "type": "string"
    }
  }
}
