# Session Sync Plan
> Keeping Codex, Claude Code, and the Kali VM aligned during the V1 -> V2 transition

Use the repo docs, schemas, and `shared/` state as the source of truth.

- Preserve `shared/`
- Replace/update box-local agent files
- Resume from Planner after migration
